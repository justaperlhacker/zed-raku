# Project: Zed Raku language extension

A local (and eventually publishable) Zed extension providing real Raku
language support: tree-sitter syntax highlighting, indentation, bracket
matching, outline, text objects, and (optionally) RakuNavigator LSP.

Repo:      ~/Projects/zed-raku
Status:    IN PROGRESS (M1-M3 scaffolded, LSP wired; pending in-Zed install)
Owner:     johnm

---

## 1. Background / motivation

- Zed has no Raku (formerly Perl 6) support in the registry or built-ins.
- The only usable tree-sitter grammar is `acrion/tree-sitter-raku`, a fork of
  `tree-sitter-perl` with "initial" Raku adjustments (most notably hyphens in
  identifiers). It ships Helix queries.
- `tree-sitter-perl/zed-perl` is a complete, working Zed language extension
  and is our scaffolding reference.
- Until this lands, Raku files are approximated as Perl via `file_types` in
  `~/.dotfiles/zed/.config/zed/settings.json`.

## 2. Constraints / gotchas (verified 2026-09-19)

- [ ] **Extension id must not contain "zed"/"Zed"/"extension"** (Zed rule).
      Use id `raku`. The repo/dir may be called `zed-raku`.
- [ ] The grammar repo does **not** commit `src/parser.c`; must run
      `tree-sitter generate` (CLI 0.26.9 + Node 26 present).
- [ ] Zed dev extensions require **rustup** + wasm target
      (`wasm32-wasip1`). System Rust is installed but rustup is NOT.
- [ ] Grammar quality caps output: a couple of Raku-only constructs still
      mis-highlight/error: real junctions (`|`/`&` parse as bitwise ops, so
      they don't get a junction-specific highlight), `q:to/END/` heredocs
      (Perl `<<` only), quote adverbs (`q:!c{...}`, `Q:q{...}`), unicode
      quotelike delimiters (`q«...»`), and token/rule/regex bodies are parsed
      opaquely (not highlighted as regexes). (See M1 for what is done.)
- [ ] `src/parser.c` in the grammar fork is ~55 MB (GitHub warns above 50 MB).
      Watch the 100 MB hard limit; consider trimming GLR conflicts or hosting
      the grammar differently if it grows further.
- [ ] Grammar is licensed MIT; keep attribution.

## 3. Deliverables

- Local dev extension loaded in Zed via `zed: install dev extension`.
- Syntax highlighting for the Raku file set (see 5.2) + first-line shebang.
- Auto-indent, bracket matching, outline, text objects.
- (Stretch) RakuNavigator LSP integration.
- (Stretch) Published to the Zed extension registry.

## 4. Repo layout (target)

    zed-raku/
      .plan
      README.md
      extension.toml            # id = "raku", pinned grammar rev
      Cargo.toml                # zed_extension_api
      src/
        raku.rs                 # minimal Extension impl
      languages/
        raku/
          config.toml
          highlights.scm
          brackets.scm
          indents.scm
          outline.scm
          injections.scm
          textobjects.scm
      grammar/                  # (option) vendored parser.c, if not forking
      scripts/
        build-grammar.sh        # clone + tree-sitter generate + copy

## 5. Milestones

### M0 - Toolchain
- [x] Confirm `tree-sitter --version` (0.26.9), `node --version` (v26.8.2).
- [x] `git init` in ~/Projects/zed-raku; add .gitignore. (pre-existing)
- [~] rustup NOT installed; system Rust 1.98 already ships
      `wasm32-wasip1`/`wasm32-wasip2`, so cargo builds succeed. Zed may still
      want rustup at install time.

### M1 - Grammar
- [x] Decide grammar sourcing: (a) fork `acrion/tree-sitter-raku` ->
      `justaperlhacker/tree-sitter-raku`, commit generated `parser.c`, pin rev.
- [x] Clone grammar, run `tree-sitter generate`, verify `src/parser.c`.
- [x] Record upstream rev for reproducibility: upstream `5031abc8`.
- [x] Fork patches on top of upstream:
      - rev `5de6437d`: binary `~` string-concat operator; `multi`/`proto`/`only`
        declarators before `sub`/`method`.
      - rev `9a1a4aec`: typed signature params (`Int $n`, defaults, named
        `Int :$x`, slurpy `*@a`), return types (`--> T`, inside and outside the
        signature), and `given`/`when`/`default`. Subscript braces made
        `token.immediate` (Raku semantics) to disambiguate `given $x { ... }`
        from `$x{...}`.
      - rev `f905384e`: twigil variables (`$!x`, `$.x`, `@.list`, `%!map`,
        `$^x`, `$?x`, ...) and `has` attribute declarations (`has Int $.x is rw
        = 0;`).
      - rev `2414eb54`: POD scanner terminates at `=end` (nesting-aware), so
        `=begin pod ... =end pod` no longer swallows the rest of the file.
      - rev `070d051f`: `constant`/`subset` declarators, the Whatever star
        (`*`), pointy `for @a -> $x { ... }`, and angle subscripts
        (`%h<key>`).
      - rev `6b9a22ff`: Raku `.method(...)` / `.method: args` calls (`.` removed
        from the Perl concat operators; number literals now require digits
        after `.`, fixing `1..10` ranges) and paren-less `if`/`elsif`/`while`/
        `unless`/`until`/`for` conditions.
      - rev `b89f5957`: `is`/`does`/`handles` traits on class/role/package/sub/
        method/`has`; safe/meta method calls (`.?`, `.^`, `.&`, `.!`); named
        arguments (`:name`, `:name(...)`, `:name<...>`, `:$var`, `:!name`);
        sigilless variables (`my \x`).
      - rev `148563e0`: Raku `?? !!` ternary.
      - rev `bc95dafd`: all Raku phasers (`ENTER`, `LEAVE`, `KEEP`, `UNDO`,
        `FIRST`, `LAST`, `NEXT`, `PRE`, `POST`, `CATCH`, `CONTROL`, `COMPOSE`,
        `START` in addition to `BEGIN`/`CHECK`/`INIT`/`END`/`UNITCHECK`).
      - rev `d4f52e9a`: user-defined operator definitions (`sub infix:<+>`,
        `prefix:<->`, `postfix:<!>`, `circumfix:<...>`, `term:<...>`).
      - rev `217e5d25`: parameterized roles/classes (`role R[::T]`,
        `class C[::T]`).
      - rev `20b2ffe1`: reduction metaoperators (`[+]`, `[~]`, `[max]`, `[*]`,
        ...); corrected return-type syntax to match Rakudo (`--> T` inside the
        signature, `of`/`returns T` outside — the outside `-->` form is a
        syntax error in Raku).
      - rev `a3c3de1c`: zip/cross (`Z`, `X`, `Z+`, `X~`, `Z=>`) and hyper
        (`>>+<<`, `»*«`) metaoperators plus hyper method calls (`@a>>.uc`).
      - rev `83f53a20`: `but` operator and anonymous roles.
      - rev `e4cdc0b3`: private/meta methods (`method !x`, `method ^x`) and
        `submethod`.
      - rev `5213f091`: `grammar`/`token`/`rule`/`regex` declarations (opaque,
        brace-balanced regex bodies via a scanner external token).
      - rev `a44b9955`: signature parameter modifiers (`$a?`, `:$a!`,
        `where ...`, `is rw`).
      - rev `be5a4c96`: Raku compound assignment operators (`~=`, `max=`,
        `min=`, `Z=`, `X=`).
      - rev `bc34d89d`: number literals `0o17`, `0d10`, radix `:16<ff>`.
      - rev `11ef6c53`: chained subscripts (`@a[0][1]`, `%h<a><b>`).
      - rev `dfbb6142`: `Q` quote operator.
      All verified against the grammar's own test suite with no new failures.

### M2 - Extension scaffold
- [x] `extension.toml` (id `raku`, name "Raku", schema_version 1, grammars).
- [x] `Cargo.toml` (copy zed-perl: `zed_extension_api`, cdylib).
- [x] `src/raku.rs` minimal impl (+ opt-in RakuNavigator command resolution).
- [x] `languages/raku/config.toml`:
      - name = "Raku", grammar = "raku"
      - path_suffixes = raku, rakumod, rakutest, rakudoc, p6, pl6, pm6, t6, pod6, nqp
      - first_line_pattern for `#!...raku` and `#!...perl6`
      - line_comments = ["# "]
      - autoclose_before + brackets + word_characters
- [x] Added a `pod` language/grammar for embedded POD (from zed-perl).
- [ ] Load via `zed: install dev extension`; sanity check. (needs GUI)

### M3 - Queries (bulk of the work)
- [x] Port `queries/highlights.scm` (Helix) -> Zed capture names.
- [x] Add `brackets.scm` ("(" @open ")" @close, etc.).
- [x] Reuse/adapt `indents.scm`.
- [x] Adapt `textobjects.scm`, `outline.scm`.
- [x] `injections.scm` for embedded POD (`=begin pod`) + `s///e`.
- [x] `overrides.scm` for `string`/`comment` scopes (bracket `not_in`).
- [~] Manually test against a corpus of real Raku files. (smoke-tested via
      `tree-sitter query`; needs real corpus + in-editor pass)

### M4 - LSP (optional / stretch)
- [ ] RakuNavigator (Node) install + build. (manual; not on npm)
- [x] `[language_servers.raku-navigator]` in extension.toml (language "Raku").
- [x] Settings for rakuPath + includePaths (passthrough + `path` to server.js).

### M5 - Polish / publish (stretch)
- [x] README, LICENSE, attribution for tree-sitter-perl/-raku/-pod.
- [ ] CI to build the grammar/ext (if publishing).
- [ ] Submit to `zed-industries/extensions` (grammar repo must be public with
      committed `parser.c`). (fork is public + parser.c committed)

## 6. Test corpus

Collect representative files covering: scripts, modules (`.rakumod`), tests
(`.rakutest`), grammar (`regex`/`grammar`/`token`/`rule`), OO
(`class`/`role`/`method`/`multi`), `given/when`, junctions, twigils,
heredocs, POD, and `nqp`.

## 7. Risks

- Grammar covers Raku only superficially -> highlighting gaps.
- Zed query capture validation is stricter than Helix; unknown captures may
  be ignored or warn.
- rustup requirement adds setup friction; dev extensions build per machine.
- `file://` grammar URLs are machine-specific (dotfiles portability issue).

## 8. Interim approximation (done)

`~/.dotfiles/zed/.config/zed/settings.json` maps the Raku file set listed in
M2 to the existing **Perl** extension. This is intentionally temporary and
should be removed once this extension is installed, to avoid the Perl
extension claiming `.raku` etc.
